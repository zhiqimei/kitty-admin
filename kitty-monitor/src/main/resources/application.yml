server:
  port: 8000
spring:
  application:
    name: kitty-monitor
  cloud:
    consul:
      #host: localhost
      host: 192.168.2.110
      port: 8500
      discovery:
        serviceName: ${spring.application.name}	# 注册到consul的服务名称